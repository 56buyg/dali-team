import type { ToolManifest } from "@/lib/tools/registry";

const manifest: ToolManifest = {
  id: "dual-image-edit",
  name: "双图编辑",
  icon: "merge",
  description: "融合两张图片，AI 智能合成与编辑",
  category: "design",
  enabled: true,
};

export default manifest;
